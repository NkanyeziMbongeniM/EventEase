@model EventEase.Models.Venue

@{
    ViewData["Title"] = "Venue Details";
}

<h2>Venue Details</h2>

<dl class="row">
    <dt class="col-sm-2">Venue Name</dt>
    <dd class="col-sm-10">@Model.VenueName</dd>
    <dt class="col-sm-2">Location</dt>
    <dd class="col-sm-10">@Model.Location</dd>
    <dt class="col-sm-2">Capacity</dt>
    <dd class="col-sm-10">@Model.Capacity</dd>
    <dt class="col-sm-2">Availability</dt>
    <dd class="col-sm-10">@(Model.IsAvailable ? "Available" : "Unavailable")</dd>
    <dt class="col-sm-2">Image URL</dt>
    <dd class="col-sm-10">@Model.ImageUrl</dd>
</dl>

<a asp-action="Edit" asp-route-id="@Model.VenueID" class="btn btn-warning">Edit</a>
<a asp-action="Index" class="btn btn-secondary">Back to List</a>
